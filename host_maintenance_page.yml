---
 - hosts: all
   gather_facts: no
   tasks:
     - name: Hosting Maintenance Page
       script: 
         cmd: /usr/local/bin/OHS_PRE-BATCH_Restart.sh
   become: yes
   become_user: "{{ appuser }}"
   become_method: sudo
